[pytest]
testpaths = tests
# conftest.py holds the shared helpers (pump, log_text) and tests import them by
# name, so the tests dir has to be importable.
pythonpath = tests
addopts = -q --strict-markers
markers =
    integration: touches the real Win32 input queue; needs an idle machine
